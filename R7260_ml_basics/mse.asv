% mean square error algorithm, based on goodfellow book 5.1.4

load('mse_data_set.mat');
x_in = x(:, 1)';
y_in = x(:, 2)';
n = length(x_in);

x_avg = mean(x_in);
xy_avg = x_in * y_in;

a_out = inv(x_in' * x_in) * x_in' * y_in;


% function w_out = mse(x_in, y_in)
%     
%     
% end
